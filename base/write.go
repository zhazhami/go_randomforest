package base

/*
import (
	"encoding/csv"
	"os"
)

func To_csv(filepath string, content [][]interface{}) {
	f, err := os.Create(filepath)
	defer f.Close()
	w := csv.NewWriter(f)
	w.WriteAll(content)
	w.Flush()
}
*/
